# Assets

Place `Logo_Talentini.svg` in this directory.

The sidebar imports it as:
```tsx
import logoUrl from '../../assets/Logo_Talentini.svg';
```

Supported format: SVG (any dimensions — the sidebar scales it to `h-8`).
